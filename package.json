{
  "name": "lockin",
  "productName": "LockIn",
  "version": "0.2.0",
  "private": true,
  "description": "Lock the whole Mac to one task. Real Chrome/Safari blocking. Local learning.",
  "main": "desktop/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node --test test/*.test.cjs",
    "prove": "node scripts/prove-youtube-block.cjs",
    "prove:chrome": "node scripts/open-youtube-locked.cjs",
    "install-login": "node scripts/install-login-item.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "electron": "^37.4.0"
  }
}
